import{createClient}from"@supabase/supabase-js";import StartConversation from"@/components/StartConversation";export default async function P({params}:{params:Promise<{id:string}>}){const{id}=await params;const s=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);const{data:d}=await s.from("drivers").select("profile_id,experience,bio,classes,profiles(name,club)").eq("profile_id",id).single();const{data:r}=await s.from("rounds").select("*").order("event_date");if(!d)return <p>Driver not found.</p>;const p=Array.isArray(d.profiles)?d.profiles[0]:d.profiles;return <><h1>{p?.name}</h1><div className="card"><p>{d.experience}</p><p>{d.bio}</p></div><StartConversation driverId={id} rounds={r||[]}/></>}